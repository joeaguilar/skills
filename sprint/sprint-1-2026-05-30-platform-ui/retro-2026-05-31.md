# Sprint-1 Retro — 2026-05-31

## Plan vs Actual

- Planned: implement M4 read-only platform switching for the primitive tree UI.
- Actual: completed in one wave. The UI now distinguishes Claude/Codex and Local/Global state, renders platform-specific manifest paths, and loads selected-platform markdown sources.

## Friction Log

- The execution arm stalled after producing valid edits. The orchestrator verified the work, closed itr#3, and shut down the arm.

## Learnings

- Cross-platform markdown smoke tests must serve the repo root. Serving only `codex/` hides `claude/**` payload paths and gives a false negative.
- The repo's current verify gate is adequate for syntax and registry safety, but UI behavior still needs a browser smoke for platform/path work.

## Action Items

- Use shorter monitoring intervals for single-arm UI waves.
- Document repo-root serving as the expected smoke path when testing cross-platform explorer behavior.
